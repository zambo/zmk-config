# Dim direnv status messages
export DIRENV_LOG_FORMAT=$'\033[2mdirenv: %s\033[0m'

# Also rebuild if other local flake inputs change
watch_file nix/*

# Use flake
use flake

# Disable Kconfig warnings as errors to fix build issues
export KCONFIG_WERROR=n
